apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: code
  template:
    metadata:
      labels:
        app: code
  spec:
    containers:
    - name: nginx-image
      image: nginx:latest
      ports:
      - containerPort: 80